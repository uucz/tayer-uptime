Add an `assignee` parameter to the `list_tasks` function in app.py. When provided, only return tasks assigned to that person. When not provided, behavior should be the same as before. The existing `status` parameter should continue to work, and both filters should be combinable.